interface:
  display_name: "Codex Canvas"
  short_description: "Install the plugin and open a local image canvas."
  icon_small: "./assets/icon.png"
  icon_large: "./assets/icon.png"
  brand_color: "#14161B"
  default_prompt: "Use $codex-canvas to install the stable Codex Canvas plugin and open its project canvas."
policy:
  allow_implicit_invocation: true
